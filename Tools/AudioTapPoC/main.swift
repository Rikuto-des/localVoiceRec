import Foundation
import AudioTapKit

/// Phase 0 PoC — Core Audio process tap で 2ch (mic + system audio) を取得する技術検証 CLI。
///
/// **S1 で Audio Specialist が中身を書きます。** S0 では起動確認のみ。
@main
struct AudioTapPoC {
    static func main() async {
        print("AudioTapPoC — placeholder. Phase 0 PoC は S1 で実装します。")
        print("AudioTapKit: \(AudioTapKit.placeholder)")
    }
}
