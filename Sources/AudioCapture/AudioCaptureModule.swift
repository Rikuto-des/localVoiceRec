import Foundation
import Contracts

/// AudioCapture モジュールのエントリ。`AudioCaptureService` を実装する。
/// **S2-A で Audio Specialist が中身を書きます。** S0 では型シェルだけ。
public enum AudioCaptureModule {
    public static let placeholder: String = "AudioCapture module — to be implemented in S2-A"
}
